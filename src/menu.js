// TODO
function createMenu() {
    const mainDiv = document.getElementById('home');
    mainDiv.removeChild(mainDiv.firstChild);
    mainDiv.innerHTML = 'Menu coming soon';
}

export default createMenu;